import { BottomNavigation, Typography } from "@mui/material";
import React from "react";
const Footer = () => {
  return (
    <BottomNavigation>
      <Typography mt={2}>Developed by Han Chien Leow</Typography>
    </BottomNavigation>
  );
};

export default Footer;
